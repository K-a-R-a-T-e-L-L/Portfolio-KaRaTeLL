import style from './styles.module.scss';

const Resume = () => {
    return (
        <>
            <article className={style.article}>Resume</article>
        </>
    );
};

export default Resume;