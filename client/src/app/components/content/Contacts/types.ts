export type ArrayContactsType = {
    link: string,
    title: string,
    display: boolean
}[];

export type InputsDataType = {
    name: string,
    contacts: string,
    message: string
}